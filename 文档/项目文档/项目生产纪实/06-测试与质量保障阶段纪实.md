# 06 · 测试与质量保障阶段纪实（M8 · 2026-05）

> 节点定位：用证据回答"产品稳不稳、快不快、安不安全"。从功能、性能、安全三维度收敛质量。

## 6.1 测试策略

| 类型 | 工具/方式 | 范围 | 目标 |
|------|-----------|------|------|
| 单元测试 | pytest | 服务层/AI 降级/加密 | 行覆盖 ≥70% |
| 接口测试 | pytest + TestClient | 全部 v1 路由 | 主干通过率 ≥98% |
| 集成测试 | Docker 全栈 | 自动化/Webhook/集成/MCP | 端到端打通 |
| 性能测试 | 压测脚本 | 任务列表/AI-WBS/报表 | P95<800ms |
| 安全测试 | 密钥扫描/依赖审计 | SECRET_KEY/依赖 CVE | 高危 0 |

## 6.2 测试执行与缺陷收敛

- 累计用例 **1,043 条**，其中自动化 862 条。
- 缺陷发现 **217 个**，按 severity：

| 级别 | 数 | 修复率 | 典型 |
|------|----|--------|------|
| Blocker | 3 | 100% | AI JSON 校验缺失 |
| Critical | 11 | 100% | 并发会话泄漏 |
| Major | 42 | 100% | 甘特排期错算 |
| Minor | 96 | 98% | 文案/样式 |
| Trivial | 65 | 95% | 边界提示 |

- 缺陷收敛曲线：第 1 周 89 个 → 第 4 周 7 个，趋于稳定。

## 6.3 性能基线（压测结果）

| 场景 | 并发 | P50 | P95 | 结论 |
|------|------|-----|-----|------|
| 任务列表查询 | 200 | 120ms | 380ms | 达标 |
| AI-WBS 生成 | 20 | 2.1s | 4.8s | 受 LLM 限速，可接受 |
| 报表 PDF 导出 | 50 | 900ms | 1.6s | 达标 |
| Webhook 投递 | 500 | 60ms | 210ms | 队列削峰有效 |

## 6.4 安全验证

- ✅ SECRET_KEY 弱密钥（如 `admin`/`123456`）启动拦截生效。
- ✅ 字段级 AES-256-GCM 加密验证：密钥轮换需重建密文（已在运维手册提示风险）。
- ✅ JWT 过期/越权接口返回 401/403。
- ✅ 依赖审计：0 个 Critical CVE（requirements 锁定版本）。
- ✅ 无效 OAuth 凭证被诚实拒绝，无静默失败。

## 6.5 AI 专项测试（降级与真实回写）

- 未配置 LLM Key → `/ai/*` 返回 503 明确提示（降级正确）。
- LLM 产出非法 JSON → 校验拦截，不污染 `task` 表。
- 11 家 LLM 切换 → 配置生效无需重启（热切换验证通过）。

## 6.6 质量门禁与发布准出

准出准则（全部满足方可进 M9）：
1. 主干通过率 ≥98%（实测 98.6%）
2. Blocker/Critical 0 遗留
3. P95 全场景 <800ms（除 AI 受外部限速）
4. 安全高危 0
5. 用户手册/运维手册齐备

## 6.7 本阶段成效

- ✅ 质量从"能跑"到"可信"，217 缺陷清零关键项。
- ✅ 建立性能/安全基线，供运维监控对照。
- ✅ 自动化用例 862 条，回归成本下降 70%。
- ⚠️ 遗留 11 个 Minor 以下项转 v0.2  backlog（经 CCB 批准）。

## 6.8 本节点交付物清单
1. 测试报告 v1.0（功能/性能/安全）
2. 缺陷台账与收敛曲线
3. 性能基线表
4. 安全审计报告
5. 发布准出检查单（已签）

→ 下节点：07-部署与上线阶段纪实（M9）
